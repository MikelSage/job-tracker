<h1>All Jobs Sorted By <%= params[:sort].capitalize %></h1>

<% @sorted_jobs.each do |key, jobs| %>
  <h3><%= "#{key}:" %></h3>
  <ul>
    <% jobs.each do |job| %>
      <li><%= job.title %></li>
    <% end %>
  </ul>
<% end %>
